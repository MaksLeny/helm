# Helm — зависимости
# Python 3.12 (winsdk требует именно 3.12)

# Ядро
fastapi
uvicorn[standard]
# Опционально: загрузка файлов/картинок с телефона в «Буфере обмена».
# Без него буфер работает (текст, история, авто-слежение), но кнопка
# прикрепления файла будет скрыта.
python-multipart

# Плагин «Мультимедиа» (WinWave)
httpx
winsdk
pycaw
comtypes
yandex-music
aiohttp        # нужен библиотеке yandex-music (иначе откат на httpx)

# Опционально: HTTPS-режим (самоподписанный сертификат для гироскопа/push).
# Без него Helm работает по HTTP; либо используйте системный openssl.
cryptography

# Плагин «Монитор системы»
psutil

# Опционально: список и переключение окон в разделе «Окна» (Windows).
# Без него «свернуть всё»/«восстановить» работают, а список окон пуст.
pywin32; sys_platform == 'win32'

# Плагины «Питание», «Приложения», «Буфер обмена» — только stdlib, без зависимостей.
